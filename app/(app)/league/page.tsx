import { PlaceholderScreen } from "@/components/layout/placeholder-screen";

export default function LeaguePage() {
  return (
    <PlaceholderScreen
      title="League"
      blurb="The live leaderboard — total points and per-mode subtotals, reordering as results land."
      phase="Phase 1"
    />
  );
}
