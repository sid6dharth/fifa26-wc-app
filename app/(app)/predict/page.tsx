import { PlaceholderScreen } from "@/components/layout/placeholder-screen";

export default function PredictPage() {
  return (
    <PlaceholderScreen
      title="Predict"
      blurb="Enter match scores with the stepper card, lock in before kickoff, and earn points."
      phase="Phase 1"
    />
  );
}
